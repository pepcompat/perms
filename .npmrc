# Electron + native modules (better-sqlite3, node-pty) ทำงานดีกับ layout แบบ hoisted
# (ไม่ใช่ symlink store ของ pnpm) — กันปัญหา resolve native addon และ electron-builder
node-linker=hoisted
# ให้ electron ดึง binary ผ่าน registry ปกติ
public-hoist-pattern[]=*electron*
